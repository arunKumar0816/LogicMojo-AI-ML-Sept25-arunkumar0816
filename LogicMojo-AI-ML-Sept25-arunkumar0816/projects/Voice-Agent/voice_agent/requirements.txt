# ── Core Voice ────────────────────────────────────────────────
SpeechRecognition>=3.10.0
pyaudio>=0.2.14            # microphone backend for SpeechRecognition
pyttsx3>=2.90              # offline TTS (Windows/macOS/Linux)
gTTS>=2.5.1                # Google online TTS (alternative)
pygame>=2.5.0              # audio playback for gTTS mp3

# ── Whisper (optional, heavier) ────────────────────────────────
openai-whisper>=20231117   # pip install openai-whisper
soundfile>=0.12.1          # used when piping mic audio to Whisper

# ── LLM / AI ──────────────────────────────────────────────────
requests>=2.31.0           # Ollama REST API calls
# google-generativeai>=0.5  # uncomment if using Gemini instead

# ── Data / Analytics ──────────────────────────────────────────
pandas>=2.2.0
openpyxl>=3.1.2            # pandas Excel export

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0       # optional: load .env for API keys
